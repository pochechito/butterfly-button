import { FormMutableDirective } from './form-mutable.directive';

describe('FormMutableDirective', () => {
  it('should create an instance', () => {
    const directive = new FormMutableDirective();
    expect(directive).toBeTruthy();
  });
});
